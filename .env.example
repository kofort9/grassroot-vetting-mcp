# ProPublica API doesn't require authentication
# These are optional configuration settings

# Enable debug logging (optional)
DEBUG=nonprofit-mcp

# Rate limiting delay in ms between API requests (default: 500)
PROPUBLICA_RATE_LIMIT_MS=500

# CourtListener API token (optional — enables court record red flag checks)
# Get a token at https://www.courtlistener.com/sign-in/
COURTLISTENER_API_TOKEN=

# Max age in days before IRS/OFAC data is re-downloaded (default: 7)
DATA_MAX_AGE_DAYS=7
